# 🐛 Bug Tracker

What broke and what shipped to fix it, newest first — [release notes](../release_note/README.md).

<!--
  HOW TO LOG A BUG (not rendered — this page is the reader's, not the author's)

  1. Copy _TEMPLATE.md to BUG-<NNNN>-<YYYY-MM-DD>-<slug>.md, where <NNNN> is the next free ID.
     scripts/docs/check_bug_ids.sh fails on a number already used by another file.
  2. Fill in the meta table + Symptom / Root cause / Fix / Verification. The H1 is the one line
     this index shows, so write it as a sentence a reader understands on its own.
  3. Do NOT edit the list below by hand — it is generated:
         python3 scripts/docs/build_bug_index.py
     Titles come from each file's H1; the build a fix shipped in comes from the `Fixed in` column
     already in this file, which the generator preserves.
  4. Leave `Fixed in` as "Unreleased" while you work. NEVER copy a number out of VERSION.txt:
     that counter bumps on every commit and every branch carries its own series, so it never
     names a release (BUG-0122 once shipped with `build 9062`, which was a worktree's counter).
     The build is set at cut time, by the release note heading the fix ends up under.
  5. When the fix ships, add the one-line entry to the release note under Bug fixes, linking
     back to the file, and set Fixed in to that section's build. scripts/docs/check_bug_meta.py
     (run by CI) fails if the two disagree, if the build was never cut, or if a file says Fixed
     with nothing in the note.
  6. Optional: reference the ID in the commit subject, e.g. `fix(db): index kpi lookup (BUG-0001)`.

  Status vocabulary: Open · In progress · Fixed (pending deploy) · Closed

  THIS TRACKER IS PUBLISHED at /docs/bugs. Write every report as if a stranger will read it — no
  customer names, no customer site hostnames, no lab addresses, no credential values. Convention:
  docs/agent/release/ANONYMIZATION.md (internal). The push-time leak gate catches the identifier
  half; the rest is on the author.

  IDs are never reused. BUG-0007 exists only on feat/demo; on main it is intentionally absent —
  the colliding parallel entry there was renumbered to BUG-0008.
-->


<!-- generated by scripts/docs/build_bug_index.py — do not edit below -->

## Unreleased

- **An adb verification silently drops leading and trailing spaces from its search term** · reported 2026-09-17 · [BUG-0136](BUG-0136-2026-09-17-adb-verification-strips-search-term-whitespace.md)
- **The auto-sign token was published to the internet in the frontend bundle** · reported 2026-09-17 · [BUG-0134](BUG-0134-2026-09-17-auto-sign-token-published-in-the-frontend-bundle.md)
- **VNC preview cards render black: centering pushes the scaled iframe out of view** — VNC previews are black cards: the iframe's layout box stays 1440×847 and scales from its top-left, so centring it moved the render off the card · reported 2026-09-15 · [BUG-0104](BUG-0104-2026-09-15-vnc-preview-scaled-off-screen-by-centering.md)
- **vpt-pi1 keeps disappearing from the UI; a Cloudflare Tunnel fixed the 522s but NOT the reported symptom** — `vpt-pi1` intermittently vanishes from the UI — **root-caused 2026-09-15**: the host is *evicted from the server registry* (9 gaps >180s in one morning, ~70 min absent). The ping ran ~7 sequential WAN DB calls **before** pinging, each on a fresh TCP connection (httpx keepalive 5s vs 60s cycle), and the Pi's outbound SYNs are dropped by the router — with postgrest's 120s default timeout one dropped SYN stalled the ping thread for minutes. Fixed: metrics moved to their own thread, DB client given a 5s connect timeout + connect-retries + 300s keep-alive, ping interval 60s→30s · reported 2026-09-15 · [BUG-0093](BUG-0093-2026-09-15-rpitest-unreachable-tunnel-did-not-fix-it.md)
- **A running script is indistinguishable from one that failed instantly** · reported 2026-09-15 · [BUG-0090](BUG-0090-2026-09-15-script-results-completed-at-set-at-insert.md)

## build 9369 — 2026-09-26

- **`docker-compose.yml`'s Supabase include can't find its env file (path doubles)** · reported 2026-09-26 · [BUG-0163](BUG-0163-2026-09-26-docker-compose-include-env-file-path-doubling.md)

## build 9364 — 2026-09-26

- **Docker stack can't start: quay.io/minio/\* refuses anonymous pulls** · reported 2026-09-26 · [BUG-0162](BUG-0162-2026-09-26-minio-quay-anonymous-pull-blocked.md)
- **Dailymotion script: missing Execution Summary + placeholder URL crash** · reported 2026-09-24 · [BUG-0161](BUG-0161-2026-09-24-dailymotion-script-missing-summary-and-placeholder-url.md)

## build 9307 — 2026-09-24

- **VNC service could not create its X11 authority file** · reported 2026-09-23 · [BUG-0160](BUG-0160-2026-09-23-vnc-service-home-owned-by-root.md)
- **Dotted image reference names fail verification** · reported 2026-09-23 · [BUG-0159](BUG-0159-2026-09-23-dotted-image-reference-name-cannot-resolve.md)
- **Backend Host stalls when starting with no configured devices** · reported 2026-09-23 · [BUG-0158](BUG-0158-2026-09-23-empty-backend-host-startup-stalls.md)
- **Standby KPI runs lost the selected mode's display name** · reported 2026-09-23 · [BUG-0157](BUG-0157-2026-09-23-standby-kpi-display-name-ignored.md)
- **Socket.IO accepted any client, on every namespace** · reported 2026-09-22 · [BUG-0156](BUG-0156-2026-09-22-socketio-namespaces-accept-any-client.md)
- **Host callbacks and the Slack webhook accept anything that reaches them** · reported 2026-09-22 · [BUG-0155](BUG-0155-2026-09-22-host-callbacks-and-slack-webhook-accept-forged-requests.md)
- **The permission matrix API answers differently from the permission checks it describes** · reported 2026-09-22 · [BUG-0154](BUG-0154-2026-09-22-workspace-permissions-missing-from-the-backend-vocabulary.md)
- **the KPI report's "Last Action" was always `N/A`** · reported 2026-09-21 · [BUG-0153](BUG-0153-2026-09-21-kpi-report-last-action-always-na.md)
- **Picking "base" in Run Tests ran the device's `.env` variant instead, and navigation lost its path** · reported 2026-09-21 · [BUG-0152](BUG-0152-2026-09-21-explicit-base-variant-dropped-device-default-wins.md)
- **Every async execution waited 2 minutes: the host's completion events were 401'd in silence** · reported 2026-09-21 · [BUG-0151](BUG-0151-2026-09-21-host-execution-events-rejected-401-since-server-lockdown.md)
- **`install_host.sh` wrote an empty VNC password file on Raspberry Pi OS and carried on** · reported 2026-09-21 · [BUG-0150](BUG-0150-2026-09-21-vncpasswd-binary-name-leaves-empty-password-file.md)
- **The dashboard's "errors only" filter outlived the errors and left an empty page** · reported 2026-09-21 · [BUG-0149](BUG-0149-2026-09-21-errors-only-filter-outlives-the-errors.md)
- **The security dashboard claimed to scan Python dependencies for CVEs and never did** · reported 2026-09-17 · [BUG-0148](BUG-0148-2026-09-17-python-dependency-cves-never-scanned.md)
- **Typing the server URL left the app in open mode, with no real login and no JWT** · reported 2026-09-17 · [BUG-0145](BUG-0145-2026-09-17-typed-server-url-leaves-the-app-unauthenticated.md)
- **A phone reached a different way could not see its own userinterfaces or scripts** · reported 2026-09-17 · [BUG-0144](BUG-0144-2026-09-17-device-model-string-match-hides-userinterfaces.md)
- **A signed-out server showed a padlock and "No servers connected", and never offered a login** · reported 2026-09-17 · [BUG-0144](BUG-0147-2026-09-17-signed-out-server-never-offers-a-login.md)
- **The stream access gate locked the mobile app out of every stream** · reported 2026-09-17 · [BUG-0143](BUG-0143-2026-09-17-stream-gate-locks-the-mobile-app-out-of-every-stream.md)
- **Every UI dump taken through Appium came back empty, and an empty screen is a legitimate answer** · reported 2026-09-17 · [BUG-0143](BUG-0146-2026-09-17-appium-page-source-parsed-as-zero-elements.md)
- **The mobile app took whatever rotation the device was in, so the same test produced sideways evidence on one vendor** · reported 2026-09-17 · [BUG-0142](BUG-0142-2026-09-17-mobile-app-unlocked-orientation-rotates-farm-evidence.md)
- **A farm slot that had never run stopped every other device on the host from streaming** · reported 2026-09-17 · [BUG-0141](BUG-0141-2026-09-17-new-farm-slot-blocks-every-grabber.md)
- **The stream service `source`d the host `.env`, so any value with a space was blanked and run as a command** · reported 2026-09-17 · [BUG-0140](BUG-0140-2026-09-17-stream-env-sourced-executes-values-with-spaces.md)
- **Switching workspace left the old target selected and still under control** · reported 2026-09-17 · [BUG-0139](BUG-0139-2026-09-17-workspace-switch-keeps-the-old-target.md)
- **A click on an Appium device searched a cache nothing had filled, and said the button was not there** · reported 2026-09-17 · [BUG-0138](BUG-0138-2026-09-17-appium-click-searches-an-empty-cache.md)
- **A queued API run finished, but the caller polling it was never told** · reported 2026-09-17 · [BUG-0137](BUG-0137-2026-09-17-queued-api-run-never-finishes-its-task.md)
- **The script API accepted a device id and ran the script on a different device** · reported 2026-09-17 · [BUG-0135](BUG-0135-2026-09-17-script-execute-ignores-device-id.md)
- **The daily fleet-health table stopped writing nine days ago and reported success every morning** · reported 2026-09-17 · [BUG-0133](BUG-0133-2026-09-17-fleet-health-stopped-writing-and-reported-success.md)

## build 9151 — 2026-09-17

- **Creating a workspace whose name was already used answered 500, and one leftover row disabled its tests for good** · reported 2026-09-16 · [BUG-0132](BUG-0132-2026-09-16-a-taken-workspace-slug-answered-500.md)
- **The whole events API was dead in production, and a test marker was hiding it** · reported 2026-09-16 · [BUG-0131](BUG-0131-2026-09-16-three-route-groups-were-unreachable-in-production.md)
- **A viewer can launch a script on a device** · reported 2026-09-16 · [BUG-0130](BUG-0130-2026-09-16-a-viewer-can-launch-a-script-on-a-device.md)
- **Listing agent sessions 500s once any session has run a tool** · reported 2026-09-16 · [BUG-0129](BUG-0129-2026-09-16-agent-sessions-listing-500s-on-a-live-tool-cache.md)
- **Every /api/events route answered 500 because the router called the registry with one argument too many** · reported 2026-09-16 · [BUG-0128](BUG-0128-2026-09-16-event-routes-500-on-a-stale-registry-call.md)
- **A missing import quietly removed the agent's ten navigation-tree tools** · reported 2026-09-16 · [BUG-0127](BUG-0127-2026-09-16-tree-tools-missing-import-drops-ten-agent-tools.md)
- **One 404 on an agent-runtime route kills Atlas chat for the life of the worker** · reported 2026-09-16 · [BUG-0126](BUG-0126-2026-09-16-runtime-route-background-loop-kills-atlas-chat.md)
- **A missing stream reports "blocked by CORS policy" instead of 404, so one dead device looks like a broken site** · reported 2026-09-15 · [BUG-0125](BUG-0125-2026-09-15-stream-errors-answer-without-cors-headers.md)
- **Every long mobile page clips its last ~56px under the bottom nav** — Every mobile page taller than the screen loses its last ~56px behind the fixed bottom nav. The container reserved 64px for the nav but `flex: 1` + `minHeight: 0` capped it at the viewport, so a long page overflowed its own box and the reservation ended up *above* the escaping content. Measured on an S21: 48px of scroll for 88px of overhang · reported 2026-09-16 · [BUG-0124](BUG-0124-2026-09-16-mobile-pages-clip-their-last-content-under-the-bottom-nav.md)
- **Every frontend restart rebuilds from scratch with the site down, because the doc cache can never be written** — `ExecStartPre=npm run build` rebuilt docs + the whole Vite bundle with `serve` already stopped, so every restart was 4m20s of hard downtime — and the doc cache meant to avoid it could never be written: steps 2-4 shared one 120s budget, the security scan was always the one killed by it, and any step's failure set `DOC_OK=0` which vetoed the snapshot for all four, so `docs.hash` never advanced. Build now happens before the restart into `dist.new` + swap, and each doc step owns its own hash and budget · reported 2026-09-15 · [BUG-0123](BUG-0123-2026-09-15-frontend-restart-rebuilds-with-the-site-down.md)
- **The motion check measures below a portrait player's video** — `WaitForVideoToAppear` failed for a minute at a time on a paired phone playing a video that was plainly visible in its own stream. `detect_motion` defaults to the centre 60% of the frame — right for a TV, wrong for a portrait phone, where YouTube draws the player across the top ~28% and fills the middle with title, Subscribe and comments. Measured on the phone: 0.9-2.9% in the centre against 4.1-7.6% over the player, with the threshold at 3.0%. The playback verifications also had no way to be pointed elsewhere — `detect_motion` took an `area`, but nothing forwarded one. Fixed: `area` plumbed through both playback commands and declared as a param, and set on the youtube-android-mobile player node · reported 2026-09-16 · [BUG-0122](BUG-0122-2026-09-16-motion-check-measures-below-a-portrait-players-video.md)
- **A feed selector opens the ad instead of the video** — A `goto` to YouTube's `video_player` on a paired phone opened an advertiser's web page instead of a video. The feed's first card was a promoted one and it carries a duration like any other, so the tree's `' seconds'` selector matched it — and nothing in the selector syntax could say "not an ad". Fixed: a `!term` prefix now disqualifies a match, excluding by screen area so the ad's unlabelled children go with it; the edge scrolls past the promoted slot and no longer falls back to a blind coordinate tap; the report names the node a click landed on; and a run's UI dump traces upload beside `execution.txt` · reported 2026-09-16 · [BUG-0121](BUG-0121-2026-09-16-youtube-selector-opens-the-sponsored-card.md)
- **`traceroute` was never installed on runner hosts, and had drifted off an existing host** — `test_scripts/gw/superping.py`'s traceroute step failed with `traceroute command not found` — `install_host.sh` never installed `traceroute` for runner-type hosts, and several full hosts across both VirtualPyTest environments had drifted and lost it. Fixed: added `traceroute` to the runner package list and installed it wherever it was missing · reported 2026-09-16 · [BUG-0120](BUG-0120-2026-09-16-runner-hosts-never-got-traceroute.md)
- **A code comment's backticks silently disabled every click in every script report** — Every script report's clickable elements — step rows, section toggles, screenshot/video modals — silently stopped responding to clicks. A code comment inside the video-modal's JS template literal used backticks around `min-width: 800px`/`height: auto`, and a backtick inside a template literal closes it: the comment's own backtick ended the string early, leaving the rest as invalid JavaScript and breaking every `onclick` handler defined in the report's single inline `<script>` block, video or not. Fixed by rewording the comment to single quotes · reported 2026-09-16 · [BUG-0119](BUG-0119-2026-09-16-report-video-modal-comment-breaks-every-report-click.md)
- **An API caller's scripts all run at once on a busy device; the browser's get queued** — Scripts fired at one device through the API all **run at once** — twenty starts, zero refusals — while the same batch from the Run Tests page queues correctly. The server never queued; the page's "Queued" is the frontend turning a `423` into a one-shot deployment. And whether the server refused at all hinged on an accidental fork in the lock manager: same user + same session was refused, same user + *different* session **took over the running script's lock**. A browser keeps one session; an API caller gets a fresh one per request, so every call superseded the last. Fixed: a running script's lock is never taken over implicitly (`force_unlock` is the explicit way), and on a conflict the server now queues the run itself as a one-shot deployment, keeps the caller's `task_id` open and completes it with the real result when the run happens — so an API caller gets exactly what the browser gets · reported 2026-09-16 · [BUG-0118](BUG-0118-2026-09-16-api-callers-run-scripts-on-a-busy-device-instead-of-queueing.md)
- **The tablet emulator crash-looped on its own disk check, then spent 90 minutes in the boot animation** — After the emulator-audio rollout restarted the fleet, **labox-tablet sat 90 minutes on the boot logo** and labox-mobile showed "System UI isn't responding". The tablet had first crash-looped five times on the emulator's own free-space check (30 GB disk, 6.1 GB free), and both AVDs ran one guest core — `avdmanager`'s default — under 45–70 % CPU steal, so zygote verified bytecode at 25–44 bytecodes/s. Fixed: tablet disk 50 GB, both AVDs `hw.cpu.ncore = 4` (tablet now boots in 45 s, mobile in 2 min), `hide_error_dialogs=1` so a boot-storm dialog no longer sits on the capture; the installer writes 4 cores / 576M heap for new AVDs · reported 2026-09-16 · [BUG-0117](BUG-0117-2026-09-16-tablet-emulator-crash-loops-on-disk-check-then-never-boots.md)
- **The Host filter on the host monitoring dashboard scans every metric row** — **System – Host Monitoring** stalled for seconds on every open: its Host filter ran `SELECT DISTINCT host_name` over the whole per-minute metrics table (913K rows on the lab, no time bound), a parallel seq scan of 2.6 s cold, while every panel renders in about a millisecond. Fixed: the variable walks the host_name index one host at a time with a recursive CTE (loose index scan), 733 ms → 32 ms, same list · reported 2026-09-16 · [BUG-0116](BUG-0116-2026-09-16-host-filter-scans-every-metric-row.md)
- **Dashboards attach gateway facts per result row, so panels take seconds on a large DB** — The Home dashboard's **Service Status** overview and the DNS / Ookla / Superping dashboards took seconds on a large production DB while staying instant on ours. Every gateway-filtered panel attached the host's gateway facts with a per-row `LEFT JOIN LATERAL` on `gw_info` — one seek per result row, fine at 25K rows with a daily scan, 57K seeks plus 57K metadata fetches per load at 1.7M rows with a scan every 40 minutes. Labels were also built with `MAX()` over every row's metadata. Fixed: panels join `gw_info_latest` (one row per host, maintained by migration `20260910c`) and labels come from `executable_identity`; the largest table went from seconds to 42 ms on that DB · reported 2026-09-16 · [BUG-0115](BUG-0115-2026-09-16-dashboards-attach-gateway-facts-per-result-row.md)
- **A second web script kills the first one's browser mid-run** — A web script fails partway through with **"Page.goto: Connection closed while reading from the driver"** — Playwright's own driver process was SIGKILLed. Our Chrome launcher cleared the debug port with `lsof -ti :PORT | kill -9`, and that matches any socket with the port on *either* end, so it killed the browser **and** every Playwright driver connected to it. Each web script runs in its own subprocess and so always launches, meaning whichever of two overlapping runs got there second destroyed the first — random-looking, and on one host it killed the same victim run every cycle for hours. Fixed: the port kill is restricted to the listener so it can never reach a driver, and a script that finds a live browser on the port waits for that run to finish (up to 300s) instead of launching over it · reported 2026-09-16 · [BUG-0114](BUG-0114-2026-09-16-a-second-web-script-kills-the-first-ones-browser.md)
- **Imagefile devices never archive, so their report videos are silently truncated** — Every **imagefile** device (emulators, paired phones) archived nothing, so its report video was silently cut to the live HLS window — a 94s test came back as 30s with `missing 79.0s` in the log and nothing in the UI. The branch segmented at `hls_list_size 30` while the archiver needs 60 one-second segments to build its 1-minute MP4, so the chunk path never ran. Compounded by `setup_ram_hot_storage.sh`, which nothing calls · reported 2026-09-16 · [BUG-0113](BUG-0113-2026-09-16-imagefile-devices-never-archive-so-report-videos-are-truncated.md)
- **The first click after a deploy dead-ends on "Failed to fetch dynamically imported module"** — The first click in a tab that was open when we deployed dead-ends on **"This page hit an error — Failed to fetch dynamically imported module"**. Every route is lazy-loaded, so a deploy (which renames every chunk and swaps the whole `dist`) leaves an old tab asking for a file that is gone — normal, and `main.tsx` has always carried a reload for exactly this. What broke it: adding `RouteErrorBoundary` (`d4f2307e60`, 2026-09-15) meant React no longer rethrew the error on the window, so that listener stopped firing. Measured: pre-boundary the tab reloads itself, post-boundary it never does. Fixed: listen to Vite's own `vite:preloadError`, which fires before the failure reaches React at all, and turn the once-per-tab guard into a 30s window so a long-lived tab survives more than one deploy · reported 2026-09-16 · [BUG-0112](BUG-0112-2026-09-16-first-click-after-a-deploy-dead-ends-on-a-missing-chunk.md)
- **A device with no audio lost its entire frame analysis, and was reported as an audio incident** — A capture pipeline with **no audio input** (emulator/phone screenshot devices, `DEVICEn_VIDEO_AUDIO=null`) reports `audio: null` — and the frame-save path formatted that None with `{volume:.1f}` inside a `logger.debug` f-string, which is built whatever the log level. The TypeError landed in the handler that writes `{"analyzed": true, "error": "failed_to_save_full_data"}`, so **every frame lost its real blackscreen/freeze/localize analysis** (5855 in 30 min on labox-mobile) and the UI rendered the missing keys as a reassuring "No". The incident state machine also read `not None` as audio loss, opening a permanent audio incident (and its thumbnail uploads) on every device that cannot capture audio · reported 2026-09-16 · [BUG-0111](BUG-0111-2026-09-16-no-audio-device-loses-its-whole-frame-analysis.md)
- **A skipped minute showed yesterday's heatmap, while the Dashboard said the service was fine** — Heatmap files are a 24h circular buffer named by **HHMM only**, so a minute the processor skips leaves *yesterday's* file at that name rather than a gap. One skipped minute (`vpt-server`'s single gevent worker refused one connection -> `No hosts available for 0516`) made the page load the previous day's frame, trip its `ageInHours > 24` test, and then render it anyway — banner on top, stale mosaic below, 5 devices where the fleet now has 6. The Dashboard called the service `active` throughout, because it asked `systemctl`, which answers for the unit and not for its output. Fixed: a frame is accepted only when its timestamp matches the slot it was fetched for, no image is drawn for a rejected one, the processor records each minute's outcome and the Dashboard chip goes `stuck` after 3 minutes without a frame, and the hosts fetch retries · reported 2026-09-16 · [BUG-0110](BUG-0110-2026-09-16-heatmap-shows-yesterdays-frame-for-a-skipped-minute.md)
- **A failed incident insert uploaded a million orphan thumbnails and filled MinIO's inodes** — Heatmap stopped publishing for ~8h: `/data` on the storage VM ran out of **inodes** (2,097,150/2,097,152 used) while 13 GB of space was still free, so every MinIO PUT returned `XMinioStorageFull`. `alerts/` held 96% of all inodes (1,005,093 `alerts/freeze` objects). Cause: three hosts still ran on the anon key (`vpt-monitor` has no `EnvironmentFile`, so it snapshots `.env` at startup and they predated the Sep 8 service_role change) → `create_incident` returned None → the issue was never removed from `pending_incidents`, so every detection tick re-uploaded 3 thumbnails under a new per-second `time_key`, ~5/sec/device, forever. The 14-day ILM rules could not help — and editing them is itself a write, which a full drive refuses · reported 2026-09-16 · [BUG-0109](BUG-0109-2026-09-16-failed-incident-insert-uploads-a-million-orphan-thumbnails.md)
- **No script can drive a paired phone: the remote controller only exists inside vpt-host** — No script can drive a **paired phone**: scripts run as their own subprocess, where `features/mobile-app`'s `register(app)` never runs, so a `phone_agent` device is built with no `remote` controller and the first action fails with `Remote controller not available`. The model was also missing from `device_models`, so no navigation tree could exist for a phone in the first place · reported 2026-09-16 · [BUG-0108](BUG-0108-2026-09-16-no-script-can-drive-a-paired-phone.md)
- **Any device console was a remote desktop for the open internet, and the VNC password rode in the URL** — 🔒 **Unauthenticated remote desktop**: `/host/<name>/{vnc_lite.html,websockify}` had no session check — a WS upgrade from any internet host returned `101` + a live `RFB 003.008` handshake on 5 hosts (`host-clone-1`, `labox-web`, `labox-dongle`, `labox-mobile`, `labox-tablet`), and the served `vnc_lite.html` hardcodes `readQueryVariable('password','admin1234')`, the password all 5 still used (confirmed via `vncpasswd -f`). Gated at the proxy on `CF-Connecting-IP` (not `$remote_addr` — no `real_ip_header` exists, so that is the CDN edge); `?password=` removed from the URL and from both builders. Step 2/3 (2026-09-16): `auth_request` + minted cookie gate added, generalized to HLS streams and the phone link — see [docs/security/STREAM_ACCESS_GATE.md](../security/STREAM_ACCESS_GATE.md) · reported 2026-09-15 · [BUG-0107](BUG-0107-2026-09-15-vnc-console-and-websockify-open-to-the-internet.md)
- **Another server's devices stream from THIS server's proxy (wrong desktop, or Bad gateway)** · reported 2026-09-15 · [BUG-0106](BUG-0106-2026-09-15-cross-server-stream-urls-resolve-against-wrong-origin.md)
- **Inside the APK the app renders its web layout, with no working "This phone" tab** — Inside the APK the app renders the deployment's *website* instead of its own bundled assets — OAuth's last hop lands on the public Site URL, where Capacitor injects no bridge, so the app silently becomes a browser with the native pages unreachable · reported 2026-09-15 · [BUG-0105](BUG-0105-2026-09-15-native-app-renders-web-layout-no-this-phone-tab.md)
- **Cross-server stream URLs keep a stray `host/` segment and silently load the SPA** — Cross-server stream URLs keep a stray `host/` segment (BUG-0100 regression); the address returns 200 with the SPA's index.html so hls.js hangs on "Loading stream…" with no error · reported 2026-09-15 · [BUG-0103](BUG-0103-2026-09-15-cross-server-stream-url-keeps-stray-host-segment.md)
- **Dump UI element boxes are scaled to the wrong rect and paint over the whole page** — Dump UI highlight boxes scaled with `cover` logic against the full panel rect while the REC modal's player uses `objectFit: 'contain'`, so on a portrait phone in a landscape panel every box came out ~1.22x too big and ~1053 px too high — painting over the navbar and the remote panel — and nothing clipped them. Overlay now computes the content rect for the player's actual fit mode, both layers sit on it with `overflow: hidden`, and the modal's container dimensions follow window resizes · reported 2026-09-15 · [BUG-0102](BUG-0102-2026-09-15-dump-ui-elements-paint-outside-stream-area.md)
- **A capture whose input stalls is invisible to the watchdog: no segments for 67h, log "healthy"** — A grabber whose input stalls keeps its log fresh (same progress line forever) so neither watchdog signal fires; `vpt-pi1`/`S21x` produced no segments for ~67 h and needed a manual restart. Watchdog now also checks segment output, and all three detectors share one guarded-restart helper · reported 2026-09-15 · [BUG-0101](BUG-0101-2026-09-15-ffmpeg-watchdog-blind-to-stalled-input.md)
- **Streams and VNC of a host on another server load against the wrong proxy (502)** — Streams/VNC of a host registered on another server load against the current frontend's proxy (relative `host_url`), so every tile 502s — and a host name present on both servers silently resolves to the wrong machine. Hosts now carry their `server_url` and relative URLs resolve against it · reported 2026-09-15 · [BUG-0100](BUG-0100-2026-09-15-cross-server-host-urls-resolve-against-wrong-proxy.md)
- **One script, four different names across the UI, Grafana and the API** — One script showed **four different names**: the file name, its `[TC0xx]`-prefixed label in the UI/reports, an unrelated hand-typed Grafana dashboard title (9 of 11 single-script dashboards disagreed with the identity map), and no name at all from `GET /server/script/list`. The `[prefix] display_name` chain existed in four hand-written copies and no shared one. Fixed: shared `format_script_label()`, `items[].label` on the script list, cache invalidated on rename, dashboard titles aligned + a drift check · reported 2026-09-15 · [BUG-0099](BUG-0099-2026-09-15-one-script-four-names-across-ui-grafana-api.md)
- **`SERVER_OPEN_MODE=true` waived the server API key too, and a wrong `X-API-Key` fell through instead of failing** — 🔒 `SERVER_OPEN_MODE=true` (the installer default) waived the **server API key** as well as the browser login, so every `/server/*` route answered an unauthenticated `curl` as `admin`; a *wrong* `X-API-Key` also fell through instead of failing. Open mode now waives the login only — a non-browser call still presents `X-API-Key` when `API_KEY` is set, and a present-but-invalid key is a hard `401` · reported 2026-09-15 · [BUG-0098](BUG-0098-2026-09-15-open-mode-waives-the-api-key.md)
- **A password reset sent as a `GET` answers `200` and looks like it worked** — A password reset sent as a `GET` instead of a `PUT` (a dropped `-X PUT` in curl) changed nothing but answered `200 {"status":"ok","exists":true}` — indistinguishable from success, so the person is handed a password that was never set. The read-only status check ignored `?grafana=true` and a `password` body. Fixed: a `GET` carrying write intent returns `405` and names the `PUT` to send · reported 2026-09-15 · [BUG-0097](BUG-0097-2026-09-15-password-reset-get-answers-200-like-success.md)
- **Host registration crashed for any device without `DEVICEn_VIDEO_STREAM_PATH`** — Host registration crashed (`NameError`) for any device configured without the explicit `DEVICEn_VIDEO_STREAM_PATH` key — the fallback referenced a `host_name` that only exists in a different function. Hit by the first `phone_agent` slot added on `host-clone-1` (TASK-17). Fixed: the path is derived from the capture folder, matching every fleet `.env`'s own convention · reported 2026-09-15 · [BUG-0096](BUG-0096-2026-09-15-device-stream-path-fallback-nameerror.md)
- **capture_monitor grows to ~4.9 GB: the freeze-thumbnail cache is unbounded, and the cleanup meant to bound it clears the wrong key** — `capture_monitor` grows to **4.84 GB** (~700 MB/day). The freeze detector's disk-fallback branch appends a decoded thumbnail per cache miss with no matching `pop`, netting up to +3 images/frame, and the hourly `cleanup_old_caches()` that should bound it is called with `capture_dir` while the cache is keyed on `dirname(thumbnails_dir)` — so it frees nothing while logging "Memory cache cleanup completed". Fixed: `deque(maxlen=8)` bounds the structure itself; cleanup uses the real key. Needs a `vpt-monitor` restart · reported 2026-09-15 · [BUG-0095](BUG-0095-2026-09-15-capture-monitor-freeze-thumbnail-cache-unbounded.md)
- **Report artifacts in the capture root are never rotated; and a long-lived archiver silently runs pre-fix code** — Capture dirs grow without bound: **nothing sweeps the capture root**, so `original_with_crop_*.png` / `verification_failure_*.html` written there by verification and KPI runs accumulate forever (vpt-pi1: 5105 files / 1.8 GB back to May). Separately, `vpt-archiver` had run since 24 June with `NRestarts=0`, so the July and September cleanup fixes were on disk but never executing (1.9 GB of >30-day `reports/` still present). Fixed: `cleanup_capture_root_artifacts()` on the hourly 30-day cadence; hosts need a `vpt-archiver` restart · reported 2026-09-15 · [BUG-0094](BUG-0094-2026-09-15-archiver-never-rotates-capture-root-artifacts.md)
- **CORS reflected any origin with credentials enabled** — 🔒 CORS on `backend_server`/`backend_host` **reflected any request Origin with credentials enabled** (`origins="*", supports_credentials=True` — Flask-CORS echoes the caller's Origin back rather than a literal `*`); nginx has no IP/origin restriction of its own, so CORS was the only gate. Fixed: explicit `CORS_ALLOWED_ORIGINS` allowlist, no wildcard fallback · reported 2026-09-15 · [BUG-0092](BUG-0092-2026-09-15-cors-reflected-any-origin-with-credentials.md)

## build 8887 — 2026-09-15

- **VPT smoke scripts built the host URL from the server origin and always 404'd** · reported 2026-09-15 · [BUG-0091](BUG-0091-2026-09-15-vpt-smoke-scripts-build-host-url-from-server-origin.md)
- **A virtual script that never ran is recorded as SUCCESSFUL** · reported 2026-09-15 · [BUG-0089](BUG-0089-2026-09-15-virtual-script-materialize-failure-reported-as-success.md)
- **Customer bundles and deploys shipped the internal `docs/tasks/` and `docs/agent/` folders** — 🔒 Customer bundles and deploys **shipped the internal `docs/tasks/` and `docs/agent/` folders** (every customer's runbooks, pentest findings, lab layout); both scripts now apply the public deny-list and the bundle builder refuses an archive that still carries them · reported 2026-09-15 · [BUG-0088](BUG-0088-2026-09-15-customer-bundle-ships-internal-docs.md)
- **MinIO had no lifecycle rules at all; /data filled up and every artifact upload failed** · reported 2026-09-14 · [BUG-0087](BUG-0087-2026-09-14-minio-no-lifecycle-rules-storage-full.md)
- **Android emulator boots with a black display, stream shows nothing** — An Android emulator host **streams a black picture while every service reports healthy** — the guest boots with SurfaceFlinger composing nothing (every layer `hidden by parent or layer flag`) although the app keeps rendering, so `screencap` returns an all-zero frame. Reproducible 2/2 on restart. The screencap loop is now a script that samples for blackness and power-cycles the guest display to recover · reported 2026-09-14 · [BUG-0086](BUG-0086-2026-09-14-android-emulator-boots-with-black-display.md)
- **Ookla, Superping and DNS dashboards never showed a failed run** — The Ookla, Superping and DNS dashboards **never showed a failed run**: their detail tables hardcoded `success = true` on top of the PASS/FAIL selector, so FAIL emptied the table and All silently dropped the failures. The tables now follow the selector and show a Status and an Error column · reported 2026-09-14 · [BUG-0085](BUG-0085-2026-09-14-gw-dashboards-hide-failed-runs.md)
- **One-VM install shipped the public Supabase CLI JWT secret and left every `.env` unfilled** — 🔒 One-VM install shipped the public Supabase CLI JWT secret and left every `.env` unfilled · reported 2026-09-09 · [BUG-0084](BUG-0084-2026-09-09-vm-install-public-jwt-secret-and-unfilled-env.md)
- **Docker standalone stack could neither build nor run: `.env` copy in the Dockerfiles, wrong supervisord user, unused Postgres, no auth wiring** — Docker standalone stack could neither build nor run (Dockerfile `.env` copy, supervisord user, unused Postgres, no auth wiring) · reported 2026-09-09 · [BUG-0083](BUG-0083-2026-09-09-docker-standalone-could-not-build-or-run.md)
- **Self-hosted installer shipped Supabase's public default JWT secret** — 🔒 The self-hosted installer shipped Supabase's **public default JWT secret** in `config.toml` — forging a `service_role` token with that well-known string and sending it to any install's REST API returned `200`, full RLS bypass, no credential theft needed. A fresh install now generates its own secret; existing installs (including `.102`) still need rotating by hand · reported 2026-09-14 · [BUG-0082](BUG-0082-2026-09-14-supabase-cli-default-jwt-secret-in-tree.md)
- **Docs → Bugs shows "Error Loading Documentation"** — 🔒 **Docs → Bugs** showed *Error Loading Documentation* — `897783ff0e` stopped publishing `docs/bugs/` but left the Bugs entry in the Docs menu, and a missing docs file on an SPA returns `200` with `index.html`, not a `404`. The tracker is published again with the 14 files that named addresses, customer hostnames or a bundle name anonymized in place; a new test fails any menu entry whose section is not published · reported 2026-09-14 · [BUG-0081](BUG-0081-2026-09-14-docs-bugs-section-unpublished-error.md)
- **A `group` that cannot be created is silently discarded: `_ensure_team` swallows every error into `None` and the call still returns `200`** — A `group` that could not be created was silently discarded: `_ensure_team` swallowed every error into `None`, indistinguishable from "no team", so the route answered `200` with `"team": null` — the reason BUG-0077's never-working team insert went unnoticed. Now raises (→ `500` with detail); only `23505` is treated as idempotent · reported 2026-09-14 · [BUG-0080](BUG-0080-2026-09-14-ensure-team-swallows-errors-into-null-group.md)
- **`deployments.created_by` references `auth.users` with no `ON DELETE` action, so a user who created a deployment can never be deleted** — `deployments.created_by` referenced `auth.users(id)` with no `ON DELETE` action (defaults to NO ACTION), so any deployment row carrying a `created_by` would make that user permanently undeletable — every other FK into `auth.users` cascades. Latent: the column is NULL on all 289 rows and nothing writes it. Now `ON DELETE SET NULL` · reported 2026-09-14 · [BUG-0079](BUG-0079-2026-09-14-deployments-created-by-fk-blocks-user-delete.md)
- **Deleting a user reports `200 {"action":"deleted"}` even when the delete failed, and still removes the Grafana account** — Deleting a user returned `200 {"action":"deleted"}` even when the delete failed — the route discarded `delete_user_by_email`'s result and derived `action` only from whether the user existed beforehand, then still deleted the Grafana account, leaving the person without dashboards but with a working platform login · reported 2026-09-14 · [BUG-0078](BUG-0078-2026-09-14-user-delete-reports-success-on-failure.md)
- **External user provisioning silently drops `group`: the auto-created team insert omits `tenant_id` (NOT NULL), and the failure is swallowed into a `200`** — External user provisioning silently drops `group`: the auto-created team insert omitted `teams.tenant_id` (NOT NULL, no SQL default) so it failed with 23502, and `_ensure_team` swallowed the error into `None` — the route returned `200` with `"team": null` and the group was discarded · reported 2026-09-14 · [BUG-0077](BUG-0077-2026-09-14-provisioning-group-dropped-team-insert-no-tenant-id.md)
- **Customer bundle build aborts on an older `PIN`: the `.env` guard checks the working tree's branch, not the staged commit** — Customer bundle build aborts on an older `PIN`: the `.env` guard listed the working tree's *current branch* (staging restores it before the check) instead of the staged commit, so a `*.env.example` template the pin carried and `main` has since deleted was reported as a leak · reported 2026-09-11 · [BUG-0076](BUG-0076-2026-09-11-bundle-env-guard-checks-wrong-ref.md)
- **`gateway_info_corrected` read hits the 8 s PostgREST statement timeout every minute on the customer DB** — `gateway_info_corrected` read killed by PostgREST's 8 s statement timeout every minute on the customer DB (93K `gw_info` rows scanned + jsonb-subtracted before `DISTINCT ON`) — hosts listing waits 8 s, gateway info never attached, caller is best-effort so nothing logs — latest scan per device now kept in `gw_info_latest` by a trigger, views read the table (TASK-16) · reported 2026-09-10 · [BUG-0075](BUG-0075-2026-09-10-gateway-info-corrected-view-statement-timeout.md)
- **Grafana variables/panels fail intermittently on a large DB: Postgres parallel workers exhaust the container's 64 MB `/dev/shm`** — Grafana variables/panels fail intermittently on a large DB: parallel workers exhaust the DB container's 64 MB `/dev/shm` (`could not resize shared memory segment … No space left on device`) — `postgres` role now runs without parallel workers · reported 2026-09-10 · [BUG-0074](BUG-0074-2026-09-10-grafana-queries-fail-postgres-shm-exhausted.md)
- **Windows hosts crash-looped after the deploy: `features/` created by rsync with an unreadable ACL** — Windows hosts crash-looped after the deploy: rsync created `features/` with a Cygwin ACL the task cannot read and the optional-feature scan made that fatal — scan now tolerant, Windows push uses `--no-perms --chmod=ugo=rwX` · reported 2026-09-09 · [BUG-0073](BUG-0073-2026-09-09-windows-host-crash-loop-features-dir-acl.md)
- **Host VNC stream (Windows/Linux desktop) cropped when the remote or web panel is open** — Host VNC stream cropped on the right when the remote/web panel is open — flexbox shrank the fixed-size noVNC iframe and the scale factor was estimated (×0.8 twice) instead of measured · reported 2026-09-09 · [BUG-0072](BUG-0072-2026-09-09-vnc-stream-cropped-with-side-panel.md)
- **Per-route `@require_user_auth` ignored the global guard: 500/401 on every admin route of a no-login site** — Per-route `@require_user_auth` re-ran the JWT-only path after the global guard had already accepted the request: 500 (no secret) / 401 (secret) on every `@require_user_auth`+`@require_role` route of a no-login site — Run Command `runCommandOnHost` 500 on the customer server · reported 2026-09-09 · [BUG-0071](BUG-0071-2026-09-09-require-user-auth-ignores-global-guard.md)
- **`SERVER_OPEN_MODE=true` was silently ignored when a `SUPABASE_JWT_SECRET` was present** — `SERVER_OPEN_MODE=true` ignored when a `SUPABASE_JWT_SECRET` was present: guard took the JWT branch first, every browser `/server/*` call 401'd on a no-login site (customer delivery) · reported 2026-09-09 · [BUG-0070](BUG-0070-2026-09-09-open-mode-defeated-by-stale-jwt-secret.md)
- **`backend_server/requirements.txt` could not be installed: `h2==4.4.1` vs `hpack==4.1.0`** — `pip install -r backend_server/requirements.txt` ResolutionImpossible: `h2==4.4.1` needs `hpack>=4.2` but the file pinned 4.1.0 (seen on the customer server) · reported 2026-09-09 · [BUG-0069](BUG-0069-2026-09-09-backend-server-requirements-unresolvable-h2-hpack.md)
- **Playwright E2E reports printed the auto-sign token in stdout attachments** — 🔒 Playwright E2E reports printed the full auto-sign token in every test's stdout attachment · reported 2026-09-08 · [BUG-0068](BUG-0068-2026-09-08-e2e-report-prints-auto-sign-token.md)
- **`web-script-local-debug` CI job green while both scripts printed `SCRIPT_SUCCESS:false`** — `web-script-local-debug` CI job PASSED while both scripts printed `SCRIPT_SUCCESS:false` — runner only checked the exit code, which VPT scripts keep at 0 · reported 2026-09-08 · [BUG-0067](BUG-0067-2026-09-08-web-script-ci-job-green-on-script-success-false.md)
- **`/server/api-testing/{categories,run,quick}` always 500: `TEST_CONFIG` was never defined** — UI script names / TC prefixes read a stale platform `frontend/public/data/script_identity_map.json`; the customer's map fed only the server and was excluded from the bundle — identity maps are now overlay-deployed customer config · reported 2026-09-08 · [BUG-0066](BUG-0066-2026-09-08-api-testing-test-config-undefined-500.md)
- **Script display names / TC prefixes in the UI came from a stale platform file, never from the customer's identity map** — UI script names / TC prefixes read a stale platform `frontend/public/data/script_identity_map.json`; the customer's map fed only the server and was excluded from the bundle — identity maps are now overlay-deployed customer config · reported 2026-09-09 · [BUG-0066](BUG-0066-2026-09-09-identity-map-ui-reads-stale-platform-copy.md)

## build 8713 — 2026-09-08

- **Web-test VNC host installer never wired audio, missing `xhost +local:`** — Web-test VNC host installer never wired audio self-heal or `xhost +local:` — pulse could die silently for hours, and enabling audio surfaced an X11 auth failure · reported 2026-09-08 · [BUG-0065](BUG-0065-2026-09-08-web-host-vnc-pulseaudio-xhost-install-gap.md)
- **`SERVER_OPEN_MODE=true` left every permission-gated route answering 500** — `SERVER_OPEN_MODE=true` set no principal, so every permission/role-gated write (testcase save, campaigns, user admin) answered 500 "Configuration error" · reported 2026-09-08 · [BUG-0064](BUG-0064-2026-09-08-open-mode-gated-routes-500-no-principal.md)
- **Run Tests list fails when the optional `virtual_scripts` table is absent** — Run Tests executables list 500'd on a deployment whose DB lacks the optional `virtual_scripts` table (feature disabled, migrations never applied) · reported 2026-09-08 · [BUG-0063](BUG-0063-2026-09-08-run-tests-list-fails-without-virtual-scripts-table.md)
- **every single-user lookup 404'd: the server can't read `profiles` through RLS** · reported 2026-09-08 · [BUG-0062](BUG-0062-2026-09-08-anon-key-server-blocked-by-profiles-rls.md)
- **any signed-in user could read and write users, teams and workspaces** · reported 2026-09-08 · [BUG-0061](BUG-0061-2026-09-08-user-team-workspace-routes-had-no-role-check.md)
- **every /docs page broke because the doc pipeline deletes before it copies** · reported 2026-09-07 · [BUG-0060](BUG-0060-2026-09-07-docs-pipeline-timeout-wipes-public-docs.md)
- **Deployment scheduler never runs the right virtual-script row** — Seven backend CI tests red on every run: pathfinding GET `get_json()` 400 (real bug), report probe unauthenticated, storage health behind guard, campaign test needs OpenCV to patch · reported 2026-09-07 · [BUG-0059](BUG-0059-2026-09-07-deployment-scheduler-drops-virtual-script-id.md)
- **Seven backend CI tests failing on every run (four unrelated causes)** — Seven backend CI tests red on every run: pathfinding GET `get_json()` 400 (real bug), report probe unauthenticated, storage health behind guard, campaign test needs OpenCV to patch · reported 2026-09-07 · [BUG-0059](BUG-0059-2026-09-07-seven-standing-backend-ci-test-failures.md)
- **CI "Backend Server Unit Tests" showed success with a 404 report** — CI backend-server-tests job green with a 404 report: pytest-timeout thread method aborted pytest before the HTML was written; now signal method, per-test budget, `report: null` when absent · reported 2026-09-07 · [BUG-0058](BUG-0058-2026-09-07-ci-backend-report-missing-after-timeout-abort.md)
- **Report pages opened in a new tab get 401 after the /server/* lockdown** — Report pages opened in a new tab (CI/CD, DOM, API-testing) got 401 after the `/server/*` lockdown — session credential now mirrored into a GET-only `SameSite=Strict` cookie · reported 2026-09-07 · [BUG-0057](BUG-0057-2026-09-07-report-pages-401-new-tab-after-server-lockdown.md)
- **Security dashboard's remaining highs: path traversal and SSRF in host/server routes** — Security dashboard highs: path traversal (host av/stream/translation, server system, cicd reports) and SSRF (heatmap, JIRA, proxyImage, Postman runner) from request-controlled strings; `/source/*` deploy routes admin-gated · reported 2026-09-07 · [BUG-0056](BUG-0056-2026-09-07-path-traversal-ssrf-hardening-security-dashboard-highs.md)
- **Heatmap stopped refreshing: the processor got 401 from its own server after the /server/* lockdown** — Heatmap stopped refreshing: processor (and MCP client, agent alerts tool, DB-backup status POST) got 401 from `/server/*` after the closed-by-default guard shipped · reported 2026-09-07 · [BUG-0055](BUG-0055-2026-09-07-heatmap-processor-401-after-server-lockdown.md)
- **Every direct read of `team_members` failed with "infinite recursion detected in policy"** — Every direct read of `team_members` failed with "infinite recursion detected in policy" (team permissions never applied in the UI) · reported 2026-09-07 · [BUG-0054](BUG-0054-2026-09-07-team-members-rls-infinite-recursion.md)
- **Supabase Studio advisor: 5 security ERRORs and ~330 warnings on the production DB** — 🔒 Supabase Studio advisor: 5 SECURITY DEFINER view ERRORs, 60 RLS initplan, 50 mutable search_path, 18 multiple-policy warnings · reported 2026-09-07 · [BUG-0053](BUG-0053-2026-09-07-studio-advisor-security-performance-warnings.md)
- **Supabase Studio flags five public tables with RLS disabled** — Supabase Studio flags five public tables with RLS disabled (ai_userinterface_tasks/transitions/verifications, device_control_sessions, quality_metrics) · reported 2026-09-07 · [BUG-0052](BUG-0052-2026-09-07-rls-disabled-on-five-public-tables.md)
- **Live credentials and hard-coded auth defaults committed in the tree** — 🔒 Live credentials and hard-coded auth defaults committed in the tree (MCP secret, Supabase JWT secret, anon key, Google keys in Snyk SARIF) · reported 2026-09-07 · [BUG-0051](BUG-0051-2026-09-07-live-credentials-committed-in-tree.md)
- **CI/CD restart button restarts the wrong runner (wrong VM, wrong unit)** — CI/CD restart button restarts the wrong runner (wrong VM, wrong unit) · reported 2026-09-07 · [BUG-0050](BUG-0050-2026-09-07-cicd-restart-button-targets-wrong-runner.md)
- **`GET /server/campaigns/results` always 500s (KeyError on response shape)** — `GET /server/campaigns/results` always 500s (KeyError on response shape) · reported 2026-09-06 · [BUG-0049](BUG-0049-2026-09-06-campaign-results-route-keyerror-500.md)
- **Re-running a virtual script tries a non-existent disk file** — Re-running a virtual script tries a non-existent disk file (rerun dropped virtual_script_id) · reported 2026-09-04 · [BUG-0048](BUG-0048-2026-09-04-virtual-script-rerun-loses-id.md)
- **Rec lock badge/tooltip shows either who is running a script or the script name, never both** — Rec lock badge/tooltip shows either who is running a script or the script name, never both · reported 2026-09-04 · [BUG-0047](BUG-0047-2026-09-04-rec-lock-badge-shows-owner-or-script-never-both.md)
- **Step mosaic never appears on a real run: screenshots already deleted by the time it's built** — Step mosaic never appears on a real run: screenshots already deleted by the time it's built · reported 2026-09-03 · [BUG-0046](BUG-0046-2026-09-03-step-mosaic-never-appears-on-real-runs.md)
- **Agent Dashboard shows "Failed to load agents from API: Failed to fetch" (slash redirect built as http:// behind the proxy)** — Agent Dashboard "Failed to fetch": slash redirect built as http:// behind the proxy · reported 2026-09-03 · [BUG-0045](BUG-0045-2026-09-03-agent-dashboard-failed-to-fetch-http-redirect.md)
- **vpt smoke scripts fail on every run: host URL from the server has no scheme** — vpt smoke scripts fail on every run: host URL from the server has no scheme · reported 2026-09-03 · [BUG-0044](BUG-0044-2026-09-03-vpt-smoke-scripts-relative-host-url.md)
- **CI pipeline dead: runner offline for weeks and every report upload silently skipped since July** — CI pipeline dead: runner offline for weeks, every report upload silently skipped since July · reported 2026-09-03 · [BUG-0043](BUG-0043-2026-09-03-ci-pipeline-dead-runner-offline-reports-never-saved.md)
- **Script report: "Test Steps" header toggle never collapses the steps; expanded icon renders as ◀** — Script report: "Test Steps" header toggle never collapses the steps; expanded icon renders as ◀ · reported 2026-09-03 · [BUG-0042](BUG-0042-2026-09-03-report-steps-section-never-collapses.md)
- **BLE adapter accepts any device while already bonded; foreign central locks the STB out ("lost pairing")** — BLE adapter accepts any device while already bonded; foreign central locks the STB out · reported 2026-09-02 · [BUG-0041](BUG-0041-2026-09-02-ble-foreign-central-grabs-bonded-adapter.md)
- **Production frontend served by an internet-exposed Vite dev server; `fs.strict:false` gave arbitrary file read** — 🔒 Production frontend served by internet-exposed Vite dev server; `fs.strict:false` gave arbitrary file read · reported 2026-09-02 · [BUG-0040](BUG-0040-2026-09-02-vite-dev-server-prod-exposure-arbitrary-file-read.md)
- **BLE daemon floods the journal on every keypress; past "lost pairing" incidents unrecoverable** — BLE daemon floods the journal on every keypress; past "lost pairing" incidents unrecoverable · reported 2026-09-02 · [BUG-0039](BUG-0039-2026-09-02-ble-daemon-journal-flood-hides-incidents.md)
- **Status page shows wrong health/backup/runner info, and 403s on refresh** · reported 2026-09-01 · [BUG-0038](BUG-0038-2026-09-01-status-page-secondary-server-and-status-route-collision.md)
- **Android TV remote button overlays don't line up with the remote artwork** — Android TV remote button overlays don't line up with the remote artwork · reported 2026-08-21 · [BUG-0037](BUG-0037-2026-08-21-android-tv-remote-button-overlay-misalignment.md)
- **Conditional-edge divergence recorded as a KPI failure instead of a recovered success** — Conditional-edge divergence recorded as a KPI failure instead of a recovered success · reported 2026-07-29 · [BUG-0036](BUG-0036-2026-07-29-kpi-conditional-divergence-recorded-as-failure.md)
- **Rec lock badge intermittently missing while a script is running** — Rec lock badge intermittently missing while a script is running · reported 2026-07-29 · [BUG-0035](BUG-0035-2026-07-29-rec-lock-badge-missing-for-execution-locks.md)
- **KPI report drops the recovered hop of a conditional multi-hop path (reports FAILED with "no KPI rows")** — KPI report drops the recovered hop of a conditional multi-hop path (reports FAILED with "no KPI rows") · reported 2026-07-29 · [BUG-0034](BUG-0034-2026-07-29-kpi-conditional-multihop-recovered-edge-dropped.md)
- **Script abort kills the bash wrapper, python keeps running (force take-control)** — Script abort kills only the bash wrapper; python keeps running after force take-control · reported 2026-07-29 · [BUG-0033](BUG-0033-2026-07-29-abort-kills-bash-wrapper-python-orphaned.md)
- **Report video misses the test: COLD backfill time-shifted, seam gap silent, duration inflated** — Report video misses the test: COLD backfill time-shifted, seam gap silent, duration inflated · reported 2026-07-28 · [BUG-0032](BUG-0032-2026-07-28-report-video-cold-chunk-time-shift.md)
- **Zap runs fail on near-static live content (motion false-positive)** — Zap runs fail on near-static live content (motion false-positive) · reported 2026-07-28 · [BUG-0031](BUG-0031-2026-07-28-zap-motion-false-positive-static-content.md)
- **Goto verification fails "no unified graph loaded" when a cache clear lands mid-navigation** — Goto verification fails "no unified graph loaded" when a cache clear lands mid-navigation · reported 2026-07-28 · [BUG-0030](BUG-0030-2026-07-28-goto-verification-graph-blanked-by-cache-clear.md)
- **Standby edge picker goes disabled when a variant re-wires the into-standby edge** — Standby edge picker goes disabled when a variant re-wires the into-standby edge · reported 2026-07-28 · [BUG-0029](BUG-0029-2026-07-28-standby-edge-variant-rewire-filter.md)
- **Fleet health report flags idle VNC hosts as DEGRADED (freeze / audio loss)** — Fleet health report flags idle VNC hosts as DEGRADED (freeze / audio loss) · reported 2026-07-26 · [BUG-0028](BUG-0028-2026-07-26-fleet-report-vnc-host-false-degraded.md)
- **Clicking a bug link in the in-app Docs errors "received HTML instead of Markdown"** — Clicking a bug link in the in-app Docs errors "received HTML instead of Markdown" · reported 2026-07-24 · [BUG-0027](BUG-0027-2026-07-24-docs-viewer-relative-link-drops-section.md)
- **Device tags wiped on every service restart** — Device tags wiped on every service restart · reported 2026-07-24 · [BUG-0026](BUG-0026-2026-07-24-device-tags-wiped-on-restart.md)
- **Single-iteration KPI runs walk the device all the way back** — Single-iteration KPI runs walk the device all the way back · reported 2026-07-24 · [BUG-0025](BUG-0025-2026-07-24-single-iteration-kpi-reverse-leg.md)
- **BLE remote chases a stale duplicate bond; Resume can silently re-pair** — BLE remote chases a stale duplicate bond; Resume can silently re-pair · reported 2026-07-24 · [BUG-0024](BUG-0024-2026-07-24-ble-stale-duplicate-bond.md)
- **Editing a conditional sibling's KPI name/threshold unlinks the edge** — Editing a conditional sibling's KPI name/threshold unlinks the edge · reported 2026-07-24 · [BUG-0023](BUG-0023-2026-07-24-conditional-sibling-kpi-edit-unlinks.md)
- **Navigation edges draw in a normalized (wrong) direction; selection panel orientation flipped** — Navigation edges draw in a normalized (wrong) direction; panel orientation flipped · reported 2026-07-23 · [BUG-0022](BUG-0022-2026-07-23-nav-edges-wrong-normalized-direction.md)
- **Variant node moves don't stage, save, or survive reload** — Variant node moves don't stage, save, or survive reload · reported 2026-07-23 · [BUG-0021](BUG-0021-2026-07-23-variant-node-moves-not-staged.md)
- **Variant edits write to the server immediately (no staged Save / Discard)** — Variant edits write to the server immediately (no staged Save / Discard) · reported 2026-07-23 · [BUG-0020](BUG-0020-2026-07-23-variant-edits-not-staged.md)
- **Deleting a variant orphans its hidden-only nodes/edges** — Deleting a variant orphans its hidden-only nodes/edges · reported 2026-07-23 · [BUG-0019](BUG-0019-2026-07-23-variant-delete-orphans-hidden-rows.md)
- **KPI report drill-down links 404 (Grafana subpath)** — KPI report drill-down links 404 (Grafana subpath) · reported 2026-07-23 · [BUG-0018](BUG-0018-2026-07-23-kpi-report-drilldown-404.md)
- **Variant edge panel doesn't refresh when an action set is deleted** — Variant edge panel doesn't refresh when an action set is deleted · reported 2026-07-23 · [BUG-0017](BUG-0017-2026-07-23-variant-edge-panel-no-refresh-on-action-set-delete.md)
- **"Confirm Action" flashes when a confirm dialog closes** — "Confirm Action" flashes when a confirm dialog closes · reported 2026-07-23 · [BUG-0016](BUG-0016-2026-07-23-confirm-action-dialog-flash.md)
- **Docs viewer renders inline code as full-width blocks; shows in-repo process sections** — Docs viewer renders inline code as full-width blocks; shows process sections · reported 2026-07-23 · [BUG-0015](BUG-0015-2026-07-23-docs-viewer-inline-code-process-sections.md)

## build 8414 — 2026-07-23

- **Can't draw an edge when a hidden edge already exists between the nodes** — Can't draw an edge when a hidden edge already exists between the nodes · reported 2026-07-23 · [BUG-0014](BUG-0014-2026-07-23-onconnect-blocked-by-hidden-edge.md)
- **Deleting an edge direction under a variant scope deletes it from base** — Deleting an edge direction under a variant scope deletes it from base · reported 2026-07-23 · [BUG-0013](BUG-0013-2026-07-23-variant-edge-direction-delete-hits-base.md)
- **Deleting a userinterface orphans its reference folder in the bucket** — Deleting a userinterface orphans its reference/navigation folders in the bucket · reported 2026-07-23 · [BUG-0012](BUG-0012-2026-07-23-userinterface-delete-orphans-reference-storage.md)
- **Test Reports filters keep the previous workspace's devices after switching workspace** — Test Reports filters keep the previous workspace's devices after switching workspace · reported 2026-07-22 · [BUG-0011](BUG-0011-2026-07-22-test-reports-filters-not-reset-on-workspace-change.md)
- **Host-diagnostic script report steps show "Start: N/A End: N/A Duration: 0s"** — Host-diagnostic script report steps show "Start: N/A End: N/A Duration: 0s" · reported 2026-07-22 · [BUG-0010](BUG-0010-2026-07-22-script-steps-na-timing.md)
- **capture hot tmpfs fills up with OCR debug images the archiver can't see; stream corrupts** — Capture hot tmpfs fills with OCR debug images the archiver can't rotate; stream corrupts · reported 2026-07-21 · [BUG-0009](BUG-0009-2026-07-21-hot-tmpfs-filled-by-unrotated-ocr-artifacts.md)
- **Rec preview stuck on "Loading stream…" while the modal plays the same stream fine** — Rec preview wedged on "Loading stream…" by native-HLS fallback; modal unaffected · reported 2026-07-21 · [BUG-0008](BUG-0008-2026-07-21-hls-preview-native-fallback-wedge.md)
- **Virtual Scripts opens on an empty editor, and "New" keeps the old interface/variant** — Virtual Scripts opens on an empty editor; "New" keeps the old interface/variant · reported 2026-07-20 · [BUG-0007](BUG-0007-2026-07-20-virtual-scripts-empty-editor-and-stale-new.md)
- **Scheduled script keeps running after force take-control** — Scheduled script keeps running after force take-control · reported 2026-07-20 · [BUG-0006](BUG-0006-2026-07-20-scheduled-script-survives-force-take-control.md)
- **KPI Measurement dashboard ignores the Action Set filter** — KPI Measurement dashboard ignores the Action Set filter · reported 2026-07-20 · [BUG-0005](BUG-0005-2026-07-20-kpi-dashboard-action-set-filter-ignored.md)
- **Edge KPI "Any can pass" verification setting doesn't save** — Edge KPI "Any can pass" verification setting doesn't save · reported 2026-07-20 · [BUG-0004](BUG-0004-2026-07-20-edge-kpi-any-can-pass-not-saved.md)
- **bluetoothd restart silently wipes the BLE remote's GATT registration; every key dropped** — bluetoothd restart wipes BLE remote GATT registration; every key silently dropped · reported 2026-07-16 · [BUG-0003](BUG-0003-2026-07-16-ble-gatt-lost-on-bluetoothd-restart.md)
- **BLE remote keys silently dropped when two `hid_remote.py` daemons share one adapter** — BLE remote keys silently dropped when two `hid_remote.py` daemons share one adapter · reported 2026-07-16 · [BUG-0002](BUG-0002-2026-07-16-ble-duplicate-daemon-key-drops.md)
- **/remote-control metrics timeout on large interfaces** — /remote-control metrics timeout on large interfaces · reported 2026-07-15 · [BUG-0001](BUG-0001-2026-07-15-remote-control-metrics-timeout.md)
